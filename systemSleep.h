#ifndef _systemSleep_cpp_
#define _systemSleep_cpp_

/* Function blocking the execution of the current thread for (at least) the specified sleep_duration in ms. Used for simulating waiting time in real system. */
void sleep();

#endif 
