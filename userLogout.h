#ifndef _userLogout_cpp_
#define _userLogout_cpp_

#include "userMenu.h" 

/* Function checking if user can be signed off. If so, then changing his isloggedIn status to false. */
void logoutUser(User* loggedUser);

#endif 
